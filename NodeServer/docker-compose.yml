services:
  api:
    container_name: node_server
    build: .
    volumes:
      - ./Application:/app/Application
      - node_modules:/app/node_modules
    ports:
      - "80:3000"
    restart: no

volumes:
  node_modules: