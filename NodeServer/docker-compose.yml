services:
  api:
    container_name: node_server
    build: .
    working_dir: /app
    volumes:
      - node_modules:/app/node_modules
    ports:
      - "80:3000"
    restart: no

volumes:
  node_modules: