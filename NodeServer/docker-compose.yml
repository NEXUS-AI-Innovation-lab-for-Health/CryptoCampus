services:
  api:
    container_name: node_server
    build: .
    working_dir: /app
    volumes:
      - ../Application:/app
      - ../Application/node_modules:/app/node_modules
    ports:
      - "80:80"
    restart: unless-stopped

volumes:
  node_modules: