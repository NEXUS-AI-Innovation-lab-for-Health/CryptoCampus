services:
  api:
    container_name: node_server
    build: .
    working_dir: /app
    volumes:
      - ../Application:/app
      - node_modules:/app/node_modules
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  node_modules: